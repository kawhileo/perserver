function Index(node){
    this.node=node;
    this.init();
}
Index.prototype.init=function(){
    var self=this;
    this.node=[];
    Array.prototype.slice.call(self.node,0).array.forEach(function(item,index) {
        
    });
}